/**
 * "The Watchpoint" badge from the brand kit — lens + amber rack pupil on the
 * dark plate. Served from /logo (copied out of proxview-brand-kit-v3); the
 * `-small` variant is the 33–96px fidelity, right for the 38px render size.
 */
export function BrandMark() {
  return (
    <img
      className="brand-mark"
      src="/logo/badge-icon-small.svg"
      alt=""
      width={38}
      height={38}
    />
  );
}
